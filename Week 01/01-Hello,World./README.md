# 🌎👋 Week 01 - Hello,World
โปรแกรมนี้จะแสดงข้อความ **"Hello, world"** บนหน้าจอ เป็นโปรแกรมภาษา **C** ถึงจะเป็นงานสั้นๆ แต่เป็นจุดเริ่มของการเข้าใจ โครงสร้างและการทำงานของมัน

## 🔍 อธิบายโค้ดทีละส่วน
### 1. #include <stdio.h>
เป็นคำสั้งของคอมไพล์ **(preprocessor)** ใช้เรียกไลบรารีมาตรฐาน สำหรับการรับ-ส่งข้อมูล **(standard input/output)** เข้ามา มันจำเป็นเพราะเราต้องใช้ **printf()**


### 🤔 ทำไมจำเป็นต้องมีละ?
ส่วน **#include <stdio.h>** คือ การบอกคอมไพล์ให้ เอาไฟล์หัวข้อ (header file) ที่ชื่อ <stdio.h> เข้ามาในโปรแกรม

- **<stdio.h>** มาจากคำว่า **Standard Input/Output eader** ภายในไฟล์มีการประกาศฟังชั่นอยู่ เช่น
    + **printf()** คือ แสดงผลออกหน้าจอ
    + **scanf()** คือ รับค่าจากผู้ใช้

- - - 

### ❗ ถ้าไม่ใส่ แล้วใช้ **printf()** โปรแกรมจะเป็นยังไง?
- คอมไพล์ (compile) ไม่ผ่าน
- ขึ้นเตือนว่าไม่รู้จักฟังก์ชันนั้น (implicit declaration)

เพราะคอมไพเลอร์ยังไม่รู้ว่า **printf** คืออะไร รับพารามิเตอร์แบบไหนได้ คืนค่าอะไรมาบ้าง?

- - -

### 🛫 ทำไมต้องอยู่ "บนสุดของไฟล์"?
บรรทัดที่ขึ้นต้นด้วย # เป็นคำสั้งของคอมไพล์ **(preprocessor)** ลำดับการทำงาน คือ
- **preprocessor** ทำงานก่อน จัดการ #include,#define
- จากนั้นคอมไพเลอร์จึงเริ่มแปลโค้ดจริง

ดังนั้นต้องเขียน include ก่อนที่จะมีการเรียกใช้ฟังก์ชันต่างๆ

- - -

